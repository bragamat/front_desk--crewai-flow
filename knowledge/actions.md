Available actions are:
- Search web should be handled by SearchTopicCrew (delegate_to: "SearchTopicCrew")
- When the user asks about current events, recent information, or anything requiring web search, delegate to SearchTopicCrew
