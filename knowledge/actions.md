Available actions are:
- Search web should be handled by SearchTopicCrew
  - When delegating, use exact string: "SearchTopicCrew"
  - This handles current events, news, recent information, web searches

Important: When setting delegate_to field, use the EXACT crew name as shown above (case-sensitive)
